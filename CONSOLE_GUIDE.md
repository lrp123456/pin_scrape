# Pinterest爬虫控制台使用指南

## 功能介绍

爬虫控制台是一个图形化界面，用于发送查询请求、监控任务进度和管理爬虫任务。

## 打开控制台

### 方法1：通过托盘菜单（推荐）
1. 右键托盘图标
2. 点击"控制台"
3. 控制台窗口自动弹出

### 方法2：直接运行
```bash
python tray_app/console_gui.py
```

## 控制台界面

```
┌─────────────────────────────────────────┐
│ Pinterest爬虫控制台                      │
├─────────────────────────────────────────┤
│ [爬虫参数]                               │
│  搜索关键词: [简约风格_______________]   │
│  最大爬取数量: [50]                      │
│  最小保存数: [0] 最小点赞数: [0]         │
│  最小评论数: [0]                         │
│                                         │
│ [🚀开始爬取] [⚡后台爬取] [⏹停止任务]  │
│ [📖API文档] [📁输出目录]                │
│                                         │
│ [任务进度]                               │
│  状态: 运行中 - collecting              │
│  进度: ████████░░░░░░░░░░ 45%           │
│  任务信息: 关键词: 简约风格 | 45/100    │
│                                         │
│ [执行日志]                               │
│  [10:30:15] 开始爬取: 简约风格          │
│  [10:30:18] ✓ 后台任务已启动            │
│  [10:35:20] ✓ 爬取完成！                │
│                          [清空日志]      │
└─────────────────────────────────────────┘
```

## 功能说明

### 1. 爬虫参数设置

#### 搜索关键词
- **说明**: 要搜索的Pinterest关键词
- **示例**: "简约风格", "cat", "modern design"
- **要求**: 必填，不能为空

#### 最大爬取数量
- **说明**: 每次爬取的Pin最大数量
- **默认**: 50
- **范围**: 10-1000
- **建议**: 50-200（太大会很慢）

#### 最小保存数
- **说明**: 筛选保存数大于此值的Pin
- **默认**: 0（不筛选）
- **示例**: 100 - 只保留保存数>100的Pin

#### 最小点赞数
- **说明**: 筛选点赞数大于此值的Pin
- **默认**: 0（不筛选）
- **示例**: 50 - 只保留点赞数>50的Pin

#### 最小评论数
- **说明**: 筛选评论数大于此值的Pin
- **默认**: 0（不筛选）
- **示例**: 10 - 只保留评论数>10的Pin

### 2. 操作按钮

#### 🚀 开始爬取
**功能**: 同步爬取（阻塞模式）
- 发送请求后等待完成
- 实时显示进度
- 完成后显示结果

**适用场景**:
- 爬取数量较小（<100）
- 需要立即查看结果
- 调试测试

**流程**:
```
点击按钮 → 发送请求 → 等待完成 → 显示结果
时间: 1-10分钟
```

#### ⚡ 后台爬取
**功能**: 异步爬取（非阻塞模式）
- 任务在后台运行
- 可以继续使用控制台
- 进度实时更新

**适用场景**:
- 爬取数量较大（>100）
- 不需要立即查看结果
- 批量任务

**流程**:
```
点击按钮 → 任务启动 → 后台运行 → 进度更新
可以关闭控制台，任务继续执行
```

#### ⏹ 停止任务
**功能**: 停止当前运行的任务
- 仅在任务运行时可用
- 发送停止信号
- 清理资源

**注意**: 停止后已爬取的数据会保存

#### 📖 API文档
**功能**: 打开Swagger API文档
- 浏览器访问 http://localhost:8000/docs
- 查看所有API接口
- 在线测试API

#### 📁 输出目录
**功能**: 打开爬取结果保存目录
- 默认: `C:\Users\用户名\PinterestScraper\output`
- 包含JSON数据文件和图片

### 3. 任务进度监控

#### 状态显示
- **空闲**: 无任务运行
- **运行中 - [阶段]**: 显示当前执行阶段

**阶段说明**:
| 阶段 | 说明 |
|------|------|
| idle | 空闲 |
| initializing | 初始化 |
| starting_chrome | 启动Chrome |
| searching | 搜索中 |
| collecting | 收集数据 |
| enriching | 获取详情 |
| downloading | 下载图片 |
| completed | 完成 |

#### 进度条
- **可视化进度**: 0-100%
- **实时更新**: 每2秒刷新
- **颜色指示**: 蓝色进度条

#### 任务信息
显示详细的任务信息：
```
关键词: 简约风格 | 进度: 45/100 | 已收集 45/100 个Pin
```

### 4. 执行日志

#### 日志内容
```
[10:30:15] 开始爬取: 简约风格
[10:30:18] ✓ 后台任务已启动: 简约风格
[10:30:20] 状态: 运行中 - collecting
[10:35:20] ✓ 爬取完成！
[10:35:20] 结果: 成功
```

#### 日志功能
- **时间戳**: 精确到秒
- **状态图标**: ✓ 成功, ✗ 失败
- **自动滚动**: 最新日志可见
- **清空功能**: 一键清空所有日志

## 使用流程

### 基础使用

```
1. 打开控制台
   右键托盘图标 → 控制台

2. 设置参数
   搜索关键词: 简约风格
   最大爬取数量: 50
   最小保存数: 100

3. 开始爬取
   点击"开始爬取"或"后台爬取"

4. 监控进度
   观察进度条和状态更新
   查看日志了解详情

5. 查看结果
   点击"输出目录"查看爬取的数据
```

### 高级使用

#### 批量爬取多个关键词

**方法1: 手动切换**
```
1. 输入"简约风格"，爬取
2. 等待完成
3. 清空关键词，输入"北欧风格"
4. 再次爬取
```

**方法2: 使用后台爬取**
```
1. 输入"简约风格"，后台爬取
2. 立即修改为"北欧风格"
3. 再次后台爬取（需等待上一个任务完成）
```

#### 质量筛选

只爬取高质量图片：
```
搜索关键词: 现代简约
最大爬取数量: 200
最小保存数: 500    ← 只保留保存数>500的
最小点赞数: 200    ← 只保留点赞数>200的
最小评论数: 50     ← 只保留评论数>50的
```

**结果**: 从200个Pin中筛选出约20-30个高质量Pin

## 快捷操作

### 键盘快捷键

| 快捷键 | 功能 |
|--------|------|
| Enter | 开始爬取（在关键词输入框中） |
| Tab | 切换输入框 |
| Esc | 关闭窗口 |

### 鼠标操作

- **双击关键词输入框**: 全选文本
- **右键日志**: 复制日志内容

## 实时监控

控制台会自动监控任务状态：

- ✅ 每2秒查询API进度
- ✅ 自动更新进度条
- ✅ 实时更新状态文本
- ✅ 添加日志记录

## 错误处理

### 常见错误及解决

#### 错误1: 无法连接到API服务

**原因**: API服务未启动

**解决**:
```
1. 右键托盘图标
2. 点击"启动服务"
3. 等待服务就绪
4. 重试操作
```

#### 错误2: 端口被占用

**原因**: 8000端口被其他程序占用

**解决**:
```
方法1: 关闭占用程序
方法2: 修改API端口（配置设置中修改）
```

#### 错误3: 任务超时

**原因**: 爬取时间过长（超过600秒）

**解决**:
```
1. 减小爬取数量
2. 增加筛选阈值
3. 检查网络连接
```

#### 错误4: Chrome启动失败

**原因**: Chrome未安装或路径错误

**解决**:
```
1. 确保Chrome已安装
2. 在配置中设置正确的Chrome路径
```

## 性能建议

### 爬取数量建议

| 用途 | 建议数量 | 预计时间 |
|------|---------|---------|
| 快速测试 | 10-20 | 1-2分钟 |
| 日常使用 | 50-100 | 3-5分钟 |
| 深度爬取 | 100-200 | 5-10分钟 |
| 大量爬取 | 200-500 | 10-30分钟 |

### 筛选参数建议

| 目标 | min_saves | min_likes | min_comments |
|------|-----------|-----------|--------------|
| 高质量 | 500+ | 200+ | 50+ |
| 中等质量 | 100+ | 50+ | 10+ |
| 一般质量 | 50+ | 20+ | 5+ |
| 不筛选 | 0 | 0 | 0 |

## 数据输出

### 输出文件

爬取完成后，输出目录包含：

```
output/
├── data.json              # 完整数据（所有Pin）
├── filtered_data.json     # 筛选后数据
├── images/                # 下载的图片
│   ├── pin_001.jpg
│   ├── pin_002.jpg
│   └── ...
└── scrape_report.txt      # 爬取报告
```

### 数据格式

**data.json 结构**:
```json
{
  "query": "简约风格",
  "total_pins": 100,
  "pins": [
    {
      "id": "123456789",
      "title": "现代简约风格客厅设计",
      "description": "...",
      "image_url": "https://...",
      "saves": 520,
      "likes": 180,
      "comments": 35,
      "link": "https://pinterest.com/pin/..."
    },
    ...
  ]
}
```

## 故障排查

### 日志查看

控制台日志显示详细执行过程：

```
[10:30:15] 开始爬取: 简约风格
[10:30:16] 连接到API: http://localhost:8000
[10:30:18] ✓ 后台任务已启动
[10:30:20] 状态更新: collecting 10%
[10:31:20] 状态更新: collecting 50%
[10:32:20] 状态更新: downloading 80%
[10:33:00] ✓ 爬取完成！
[10:33:00] 结果: 成功，爬取50个Pin，筛选后15个
```

### 调试技巧

1. **查看详细日志**: 控制台显示所有操作
2. **检查API响应**: 访问 http://localhost:8000/docs 测试
3. **查看输出文件**: 确认数据是否正确保存

---

**版本**: 2.1.0
**更新日期**: 2026-04-17
