# 可视化配置面板使用指南

## 功能介绍

托盘应用现在提供图形化配置界面，无需手动编辑JSON文件即可设置所有参数。

## 打开配置面板

### 方法1：通过托盘菜单
1. 右键托盘图标
2. 点击"配置设置"
3. 配置窗口自动弹出

### 方法2：直接运行
```bash
python tray_app/config_gui.py
```

## 配置界面说明

### 1. 查询参数

设置默认的爬虫搜索条件：

| 参数 | 说明 | 默认值 | 范围 |
|------|------|--------|------|
| 搜索关键词 | 默认搜索的Pinterest关键词 | 空 | 任意文本 |
| 最大爬取数量 | 每次爬取的最大Pin数量 | 100 | 10-1000 |
| 最小保存数阈值 | 筛选保存数大于此值的Pin | 0 | 0-10000 |
| 最小点赞数阈值 | 筛选点赞数大于此值的Pin | 0 | 0-10000 |
| 最小评论数阈值 | 筛选评论数大于此值的Pin | 0 | 0-10000 |

**使用示例**：
```
搜索关键词：简约风格
最大爬取数量：200
最小保存数阈值：100
最小点赞数阈值：50

→ 爬取200个"简约风格"的Pin，只保留保存数>100且点赞数>50的Pin
```

### 2. 服务配置

API服务和输出设置：

| 参数 | 说明 | 默认值 |
|------|------|--------|
| API服务端口 | API服务的监听端口 | 8000 |
| 输出目录 | 爬取结果的保存位置 | 用户目录\PinterestScraper\output |

**注意事项**：
- API端口修改后需要重启服务
- 输出目录会自动创建
- 可以点击"浏览"按钮选择目录

### 3. Chrome配置

浏览器相关设置：

| 参数 | 说明 | 默认值 |
|------|------|--------|
| Chrome调试端口 | Chrome远程调试端口 | 9222 |
| 无头模式 | 后台运行，不显示浏览器窗口 | 否 |
| Chrome配置目录 | 保存登录状态的目录 | 空 |

**重要提示**：

#### 无头模式
- ✅ 勾选：浏览器在后台运行，不显示窗口（推荐服务器使用）
- ❌ 不勾选：会显示浏览器窗口，可以看到操作过程（调试推荐）

#### Chrome配置目录
**为什么要设置？**
- Pinterest需要登录才能完整访问
- 配置目录会保存登录状态
- 设置后只需登录一次

**如何使用？**
1. 创建一个目录，如：`C:\pinterest_chrome_profile`
2. 在配置中设置该路径
3. 首次运行时会打开浏览器
4. 手动登录Pinterest
5. 登录信息会保存，后续自动使用

**留空会怎样？**
- 每次都使用临时配置
- 需要重新登录
- 或在代码中自动处理登录墙

### 4. 其他设置

| 参数 | 说明 |
|------|------|
| 自定义图标 | 托盘图标的图片文件路径 |
| 开机自动启动 | Windows启动时自动运行应用 |

**自定义图标**：
- 支持格式：.ico, .png, .jpg, .bmp
- 推荐尺寸：64x64像素
- 保存后重启应用生效

## 操作按钮

### 保存配置
- 验证所有输入
- 保存到配置文件
- 提示哪些设置需要重启

### 重置默认
- 恢复所有设置为默认值
- 需要确认才会执行

### 取消
- 关闭窗口，不保存修改

## 使用流程

### 完整配置示例

```
1. 打开配置面板
   右键托盘图标 → 配置设置

2. 设置查询参数
   搜索关键词：现代简约
   最大爬取数量：100
   最小保存数阈值：50

3. 设置输出目录
   点击"浏览"选择：D:\PinterestData

4. 设置Chrome配置
   取消"无头模式"（首次调试）
   Chrome配置目录：C:\pinterest_profile

5. 点击"保存配置"

6. 启动服务
   右键托盘 → 启动服务

7. 首次运行登录
   会打开Chrome浏览器
   手动登录Pinterest
   登录后关闭浏览器

8. 后续使用
   自动使用保存的登录状态
```

## 配置文件位置

配置保存在：
```
%APPDATA%\PinterestScraper\config.json
```

可以直接编辑此文件（高级用户）。

## 常见问题

### Q: 修改端口后服务启动失败？

**A**: 可能原因：
1. 新端口被占用 → 更换端口
2. 防火墙阻止 → 允许应用通过防火墙
3. 需要重启服务 → 右键托盘 → 重启服务

### Q: 配置保存后没有生效？

**A**: 不同设置生效方式：
- ✅ **立即生效**：查询参数、输出目录
- ⚠️ **需要重启服务**：API端口、Chrome端口
- ⚠️ **需要重启应用**：自定义图标、开机自启

### Q: 如何恢复默认配置？

**A**: 两种方法：
1. 配置面板中点击"重置默认"
2. 删除配置文件，重启应用自动创建

### Q: 配置目录找不到？

**A**: 默认位置：
```
C:\Users\你的用户名\AppData\Roaming\PinterestScraper\
```

或运行命令：
```bash
echo %APPDATA%\PinterestScraper
```

### Q: 图形界面无法打开？

**A**: 可能原因：
1. Tkinter未安装（Python自带）
2. 显示问题

**解决方法**：
- 使用记事本编辑配置文件
- 运行 `python tray_app/config_gui.py` 查看错误

## 高级技巧

### 1. 多套配置

创建不同的配置文件：
```
config_work.json    ← 工作用
config_home.json    ← 家庭用
```

使用时复制到 `config.json`

### 2. 快速切换配置

创建批处理文件：
```batch
@echo off
copy config_work.json "%APPDATA%\PinterestScraper\config.json"
echo 已切换到工作配置
pause
```

### 3. 配置导入导出

**导出配置**：
```
复制 %APPDATA%\PinterestScraper\config.json 到其他位置
```

**导入配置**：
```
复制配置文件到 %APPDATA%\PinterestScraper\config.json
重启应用
```

## 键盘快捷键

配置面板支持标准Windows快捷键：

| 快捷键 | 功能 |
|--------|------|
| Tab | 切换到下一个输入框 |
| Shift+Tab | 切换到上一个输入框 |
| Enter | 保存配置 |
| Esc | 取消关闭 |

## 版本更新

### v2.1.0 (当前版本)
- ✅ 图形化配置界面
- ✅ 实时验证输入
- ✅ 一键重置默认
- ✅ 浏览目录功能

### 未来计划
- 配置文件管理（多配置切换）
- 导入/导出配置
- 配置模板
- 配置备份恢复

---

**更新日期**: 2026-04-17
**版本**: 2.1.0
