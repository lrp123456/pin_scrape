# 托盘图标修改功能使用说明

## 功能介绍

现在支持自定义托盘图标，可以选择自己喜欢的图标文件替换默认图标。

## 使用方法

### 1. 修改图标

1. 右键点击托盘图标
2. 选择"修改图标"
3. 在文件选择对话框中选择图标文件
4. 支持的格式：
   - `.ico` - 图标文件（推荐）
   - `.png` - PNG图片
   - `.jpg/.jpeg` - JPEG图片
   - `.bmp` - BMP图片

### 2. 应用新图标

选择图标后会提示"图标已更新！重启应用后生效。"

**方法一：重启应用**
- 右键托盘图标 -> 退出
- 重新运行 `tray_app.exe`

**方法二：立即生效（开发环境）**
- 关闭并重新运行 `python tray_app/tray_main.py`

### 3. 恢复默认图标

要恢复默认图标：
1. 打开配置文件（右键 -> 配置设置）
2. 找到 `custom_icon_path` 字段
3. 清空该字段的值（删除路径）
4. 保存配置文件
5. 重启应用

配置文件位置：`%APPDATA%\PinterestScraper\config.json`

## 图标要求

### 推荐规格
- **格式**: ICO 格式（最佳）
- **尺寸**: 64x64 像素或更大
- **颜色**: 支持 alpha 通道透明

### 注意事项
1. 图标文件应该清晰可见
2. 避免过于复杂的图案（在托盘小尺寸下不易辨认）
3. 建议使用专业的图标资源（如 iconfinder.com, icons8.com）

## 示例配置

配置文件示例：
```json
{
  "api_port": 8000,
  "custom_icon_path": "C:\\Users\\王\\Pictures\\my_icon.ico",
  "output_dir": "C:\\Users\\王\\PinterestScraper\\output",
  ...
}
```

## 常见问题

### Q: 为什么选择图标后没有立即生效？
A: 需要重启应用才能加载新图标。这是因为托盘图标在应用启动时初始化。

### Q: 支持哪些图片格式？
A: 支持 .ico, .png, .jpg, .jpeg, .bmp 格式。推荐使用 .ico 格式以获得最佳效果。

### Q: 图标显示不正常怎么办？
A:
1. 检查图标文件是否损坏
2. 尝试使用其他图标文件
3. 确认图标尺寸不要太大（建议 256x256 以下）
4. 清空配置中的 `custom_icon_path` 恢复默认图标

### Q: 如何找到好看的图标？
A: 推荐网站：
- https://www.iconfinder.com
- https://icons8.com
- https://www.flaticon.com

搜索 "pinterest" 或相关关键词即可找到合适的图标。

## 技术说明

- 图标路径保存在配置文件的 `custom_icon_path` 字段
- 支持 PIL 库识别的所有图片格式
- 图标加载失败会自动回退到默认图标
- 图标文件路径支持绝对路径和相对路径
