# Pinterest登录功能使用指南

## 功能说明

爬虫现在支持自动登录检测和持久化登录状态：

✅ **首次运行**：检测到需要登录时，提示用户手动登录，等待登录完成
✅ **后续运行**：自动使用保存的登录状态，无需重复登录
✅ **固定配置目录**：Chrome使用固定的用户数据目录保存登录信息

## 默认配置目录

```
Windows: C:\Users\<用户名>\AppData\Local\PinterestScraper\chrome_profile\
```

这个目录会：
- 保存Pinterest登录状态
- 保存cookies和会话信息
- 首次登录后永久保存

## 使用流程

### 首次使用（需要登录）

```
1. 启动服务
   右键托盘图标 → 启动服务

2. 发送爬取请求
   打开控制台 → 输入关键词 → 开始爬取

3. 检测到需要登录
   程序会提示：
   ════════════════════════════════════
   ⚠️  检测到需要 Pinterest 登录
   ════════════════════════════════════

   请在打开的浏览器窗口中手动登录 Pinterest
   登录完成后，程序将自动继续...

   提示：
     1. 使用邮箱/密码登录
     2. 或使用Google/Facebook账号登录
     3. 登录成功后会自动保存登录状态
   ════════════════════════════════════

4. 在Chrome窗口中登录
   - 程序会自动打开Chrome浏览器（非无头模式）
   - 手动输入Pinterest账号密码登录
   - 或使用第三方账号（Google/Facebook）

5. 登录成功
   程序自动检测登录完成：
   ✓ 登录成功！继续执行爬取任务...
   登录状态已保存，下次运行将自动使用。

6. 开始爬取
   登录后自动继续执行爬取任务
```

### 后续使用（自动登录）

```
1. 启动服务
   右键托盘图标 → 启动服务

2. 发送爬取请求
   控制台 → 输入关键词 → 开始爬取

3. 自动使用登录状态
   程序自动检测到已登录，直接开始爬取
   ✓ 使用已保存的登录状态
   开始爬取数据...

4. 完成爬取
   无需任何手动操作
```

## 登录状态管理

### 查看登录状态

**方法1：检查配置目录**
```bash
# 打开配置目录
explorer %LOCALAPPDATA%\PinterestScraper\chrome_profile

# 如果目录存在且包含文件，说明登录状态已保存
```

**方法2：观察Chrome窗口**
- 如果Chrome打开后直接显示Pinterest首页，说明已登录
- 如果显示登录页面，说明需要重新登录

### 清除登录状态

如果需要更换账号或重新登录：

```bash
# 删除配置目录
rmdir /S /Q "%LOCALAPPDATA%\PinterestScraper\chrome_profile"

# 下次运行时会要求重新登录
```

### 更换Pinterest账号

1. 清除登录状态（删除配置目录）
2. 重新运行爬虫
3. 使用新账号登录

## 注意事项

### 1. 浏览器窗口管理

**首次登录时**：
- Chrome会以**可见窗口**模式打开（非无头模式）
- 方便用户手动输入账号密码
- 登录完成后不要关闭浏览器窗口
- 程序会自动检测登录状态并继续

**后续运行**：
- 如果是后台任务，Chrome可以使用无头模式
- 登录状态已保存，不需要显示窗口

### 2. 登录等待时间

- **最长等待**：5分钟（300秒）
- **超时处理**：程序会提示重新运行
- **建议**：在1-2分钟内完成登录

### 3. 多账号场景

如果需要在多个Pinterest账号间切换：

**方法1：使用不同配置目录**
```bash
# 账号1
python main.py --chrome-profile "C:\profiles\account1"

# 账号2
python main.py --chrome-profile "C:\profiles\account2"
```

**方法2：清除重新登录**
- 删除配置目录
- 使用新账号登录

### 4. 安全提示

⚠️ **重要安全提示**：

- 配置目录包含敏感登录信息
- 不要分享或上传配置目录
- 定期检查登录状态
- 如发现异常，立即清除重新登录

## 故障排查

### 问题1：登录后仍然提示需要登录

**可能原因**：
- 登录未成功保存
- 配置目录权限问题
- Chrome配置损坏

**解决方法**：
```bash
# 1. 清除配置目录
rmdir /S /Q "%LOCALAPPDATA%\PinterestScraper\chrome_profile"

# 2. 重新运行程序
# 3. 重新登录
```

### 问题2：等待登录超时

**可能原因**：
- 5分钟内未完成登录
- 网络问题
- Pinterest登录流程卡住

**解决方法**：
1. 检查网络连接
2. 重新运行程序
3. 在Chrome窗口中快速完成登录

### 问题3：Chrome窗口未打开

**可能原因**：
- Chrome配置为无头模式
- Chrome路径错误

**解决方法**：
```bash
# 临时禁用无头模式
# 在配置中设置：chrome_headless: false
```

### 问题4：配置目录找不到

**检查方法**：
```bash
# Windows
dir "%LOCALAPPDATA%\PinterestScraper\chrome_profile"

# 如果提示找不到，说明还未创建
# 首次运行后会自动创建
```

## 高级配置

### 自定义配置目录

通过配置文件或命令行指定：

**方法1：配置文件**
```json
{
  "chrome_profile": "D:\\MyProfiles\\pinterest_chrome"
}
```

**方法2：命令行参数**
```bash
python main.py --chrome-profile "D:\profiles\pinterest"
```

### 禁用持久化（临时模式）

如果不想保存登录状态：

```bash
# 不指定配置目录，使用临时配置
python main.py  # 不使用 --chrome-profile 参数
```

但这样每次都需要重新登录，不推荐。

## 登录状态验证

### 检查是否已登录

运行测试命令：

```bash
# 运行一次小型爬取测试
python main.py -q "test" -n 5 --connect

# 如果直接开始爬取，说明已登录
# 如果提示需要登录，说明需要重新登录
```

### 验证登录账号

查看Chrome配置目录中的文件：

```bash
%LOCALAPPDATA%\PinterestScraper\chrome_profile\Default\

# 检查以下文件：
# - Cookies
# - Login Data
# - Preferences
```

## 最佳实践

### ✅ 推荐做法

1. **首次登录**
   - 使用非无头模式（chrome_headless: false）
   - 手动完成登录
   - 验证登录成功

2. **日常使用**
   - 可以使用无头模式
   - 自动使用保存的登录状态
   - 定期检查登录状态

3. **多环境管理**
   - 开发环境：使用默认配置目录
   - 生产环境：指定专用配置目录
   - 不要混用配置目录

### ❌ 避免的做法

1. **不要**删除或移动配置目录
2. **不要**在多个应用间共享配置目录
3. **不要**使用临时模式（除非测试）
4. **不要**在公共电脑保存登录状态

## 常见场景

### 场景1：个人使用

```
1. 首次运行，登录Pinterest账号
2. 后续自动使用，无需重复登录
3. 定期检查登录状态
```

### 场景2：团队使用

```
选项A：每人独立账号
- 每个人使用自己的Pinterest账号
- 独立的配置目录
- 互不干扰

选项B：共享账号
- 使用同一个Pinterest账号
- 共享配置目录（需注意权限）
- 协同工作
```

### 场景3：服务器部署

```
1. 本地首次登录，生成配置目录
2. 将配置目录复制到服务器
3. 服务器使用相同配置目录
4. 无需在服务器重新登录
```

---

**版本**: 2.2.0
**更新日期**: 2026-04-17
**功能状态**: ✅ 已实现
